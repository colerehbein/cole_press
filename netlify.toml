[build]
publish = "_site"

# Note: Build is handled by GitHub Actions, not Netlify
# GitHub Actions builds with R/Quarto and deploys to Netlify
# Auto-builds should be disabled in Netlify UI (Build & Deploy settings)
