[build]
command = "mkdir -p _site && echo 'placeholder' > _site/.gitkeep"
publish = "_site"

# Note: Actual build is handled by GitHub Actions
# This placeholder command prevents Netlify auto-build failures
# GitHub Actions builds the full site and deploys via nwtgck/actions-netlify
