---
title: "Cole Rehbein"
title-block-banner: false
page-layout: article
toc: false
---

::::: homepage-with-sidebar

:::: main-content

::: cli-prompt
<span class="text-terminal-primary">cole@press:~$</span> <code class="text-glow">neofetch --config custom</code>
:::

::::: neofetch-style
::: ascii-art
```         
     ██████╗ ██████╗ ██╗     ███████╗
    ██╔════╝██╔═══██╗██║     ██╔════╝
    ██║     ██║   ██║██║     █████╗  
    ██║     ██║   ██║██║     ██╔══╝  
    ╚██████╗╚██████╔╝███████╗███████╗
     ╚═════╝ ╚═════╝ ╚══════╝╚══════╝

          ░░░░░▒▒▒▒▒▒▒▒▒▒▒▒░░░░░
        ░░▒▒▒▓▓▓▓▓▓▓▓▓▓▓▓▓▓▒▒▒░░
       ░▒▒▓▓████████████████▓▓▒▒░
      ░▒▓▓██████▓▓▓▓▓▓██████▓▓▒░
      ▒▓████▓▓██      ██▓▓████▓▒
     ▒▓████▓▓██  ●  ●  ██▓▓████▓▒
     ▒▓████▓▓██    ◡    ██▓▓████▓▒
     ▒▓████▓▓██          ██▓▓████▓▒
      ▒▓████▓▓██  ～～～  ██▓▓████▓▒
       ░▒▓▓██████▓▓▓▓▓▓██████▓▓▒░
        ░▒▒▓▓████████████▓▓▒▒░
          ░░▒▒▒▓▓▓▓▓▓▓▓▒▒▒░░
             ░░░▒▒▒▒▒░░░
```
:::

::: system-info
**Cole Rehbein** \@ **cole.press** **──────────────────────────────────**\
**OS:** The One Great Matter\
**Kernel:** Sitting, writing, counseling, building\
**Uptime:** Twenty-four seven, three-sixty-five\
**Shell:** Zazen, markdown, R, compassion, communism\
**Resolution:** Dialectical materialism\
**DE:** The Commons\
**WM:** Cognitive reframing\
**Theme:** Impermanence\
**Icons:** Enso / Hammer\
**Terminal:** Praxis through active listening\
**CPU:** Critical mindfulness\
**Memory:** Collective experience
:::
:::::

::: cli-prompt
<span class="text-terminal-primary">cole@press:~$</span> <code class="text-glow">cat skills.json | jq '.expertise' && ls -la ~/projects/</code>
:::

:::::::::: skills-and-projects
:::: skills-section
::: skills-json
``` json
{
  "clinical_practice": {
    "focus": "Crisis intervention & counseling",
    "modalities": ["ACT", "Mindfulness-based CBT", "Behavioral activation"],
    "populations": ["988/crisis lines", "SMI", "Depression", "ADHD"],
    "languages": ["English", "Spanish (B1)"]
  },
  "research": {
    "focus": "Behavioral health & cognition",
    "methods": ["Psychological assessment", "EEG", "Statistical analysis"],
    "tools": ["R", "SPSS", "Qualtrics"],
    "thesis": "Mindfulness-based behavioral activation for depression"
  },
  "technical_writing": {
    "focus": "Academic & technical publishing",
    "tools": ["Quarto", "LaTeX", "Markdown", "R Markdown"],
    "outputs": ["Thesis", "Research papers", "This website"]
  },
  "journalism": {
    "focus": "Culture & community",
    "experience": ["City reporter", "Copyeditor", "Culture writer"],
    "beats": ["Local politics", "Food", "Arts"]
  },
  "philosophy": {
    "degree": "B.A., St. John's College",
    "focus": "Spinoza, ethics, philosophy of mind",
    "languages": ["Ancient Greek (reading)", "French (reading)"]
  }
}
```
:::
::::
::::::::::

::: cli-prompt
<span class="text-terminal-primary">cole@press:~$</span> <code class="text-glow">git log --oneline --graph | head -10</code>
:::

::: git-activity
```{r}
#| echo: false
#| warning: false
#| message: false

library(htmltools)
library(gh)

# Fetch real GitHub stats (with fallback)
get_github_stats <- function() {
  tryCatch({
    # Get user info
    user <- gh::gh("GET /users/{username}", username = "colerehbein")
    
    # Get recent events (commits, pushes, etc.)
    events <- gh::gh("GET /users/{username}/events", username = "colerehbein", per_page = 30)
    push_events <- sum(sapply(events, function(e) e$type == "PushEvent"))
    
    list(
      repos = user$public_repos,
      recent_pushes = push_events,
      success = TRUE
    )
  }, error = function(e) {
    list(repos = "??", recent_pushes = "??", success = FALSE)
  })
}

stats <- get_github_stats()

div(class = "git-log",
    div(class = "git-line", paste0("* ", stats$repos, " public repositories")),
    div(class = "git-line", paste0("* ", stats$recent_pushes, " recent pushes")),
    div(class = "git-separator", "────────────────────────"),
    div(class = "git-status active", "● Building in public"),
    div(class = "git-status active", "● Learning by doing"),
    div(class = "git-status active", "● Embracing impermanence"),
    div(class = "git-link-container",
        tags$a(href = "https://github.com/colerehbein", 
               class = "git-remote", 
               "origin/main → github.com/colerehbein")
    )
)
```
:::

::: cli-prompt
<span class="text-terminal-primary">cole@press:~$</span> <code class="text-glow">cat thoughts/latest.txt</code>
:::

:::::: thoughts-section
::: {.thought-item .philosophy}
**📜 Spinoza_and_Selfhood.md**\
Philosophy, identity, ethics—the internally determined self
:::

::: {.thought-item .praxis}
**☎️ Crisis_Praxis.py**\
Mental health, systems, solidarity—sitting with suffering
:::

::: {.thought-item .culture}
**🌶️ Enchilada_Reviews.txt**\
New Mexico food, culture writing, and strong opinions on chile
:::
::::::

::: cli-footer
``` bash
$ uptime
 10:35AM  up 365 days, sitting with suffering
$ whoami
Cole Rehbein - Crisis Counselor & Researcher
$ echo "Thanks for visiting! ☕"
Thanks for visiting! ☕
```
:::

::::

:::: connect-sidebar

::::: newsletter-box
**📬 Newsletter**

``` bash
$ curl -X POST cole.press/subscribe
```

<form action="#" method="post" onsubmit="return false;" class="cli-form">
<div class="cli-input-wrapper">
<span class="cli-prompt-small">$</span>
<input type="email" name="email" placeholder="your@email.com" required class="cli-input">
<button type="submit" class="cli-button">GO</button>
</div>
<small class="cli-note">No spam, just signal</small>
</form>
:::::

::::: social-links
**🔗 Links**

<div class="social-links-list">
<div class="social-link-row"><span class="social-label">github</span><span class="social-separator">:</span><a href="https://github.com/colerehbein">colerehbein</a></div>
<div class="social-link-row"><span class="social-label">twitter</span><span class="social-separator">:</span><a href="https://twitter.com/colerehbein">@colerehbein</a></div>
<div class="social-link-row"><span class="social-label">email</span><span class="social-separator">:</span><a href="mailto:cole@cole.press">cole@cole.press</a></div>
<div class="social-link-row"><span class="social-label">web</span><span class="social-separator">:</span><a href="https://cole.press">cole.press</a></div>
</div>
:::::

::::: project-sidebar-items
::: {.project-item .blog}
**Blog**

Writing on psychology, philosophy, political theory. The examined life, examined further.

[**Visit Blog →**](/blog){.project-link}
:::

::: {.project-item .cv}
**CV**

Clinical psychology, crisis intervention, research, journalism.

[**View CV →**](/cv){.project-link}
:::

::: {.project-item .portfolio}
**Projects**

Research, writing samples, and things I've built.

[**Explore Projects →**](/projects){.project-link}
:::
:::::

::::

:::::

```{=html}
<script>
document.addEventListener('DOMContentLoaded', function() {
    // Enhanced hover effects with smooth transitions
    const interactiveElements = document.querySelectorAll('.project-item, .thought-item, .newsletter-box');
    interactiveElements.forEach(el => {
        el.style.transition = 'all 0.3s cubic-bezier(0.4, 0, 0.2, 1)';
        
        el.addEventListener('mouseenter', function() {
            this.style.transform = 'translateY(-2px) scale(1.02)';
            this.style.boxShadow = '0 8px 25px rgba(0,0,0,0.15)';
        });
        el.addEventListener('mouseleave', function() {
            this.style.transform = 'translateY(0) scale(1)';
            this.style.boxShadow = '';
        });
    });
    
    // Typewriter effect for CLI prompts when they scroll into view
    const cliPrompts = document.querySelectorAll('.cli-prompt code');
    const completedPrompts = new Set();
    
    const typewriterObserver = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                const code = entry.target;
                const text = code.textContent;
                const typingSpeed = 30;
                const totalTypingTime = text.length * typingSpeed + 200; // 200ms initial delay
                
                code.textContent = '';
                code.style.opacity = '1';
                
                // Start typewriter after a short delay
                setTimeout(() => {
                    let i = 0;
                    function type() {
                        if (i < text.length) {
                            code.textContent += text.charAt(i);
                            i++;
                            setTimeout(type, typingSpeed);
                        } else {
                            // Mark this prompt as completed
                            completedPrompts.add(code);
                            
                            // Trigger any dependent sections to fade in
                            const nextSection = code.closest('.cli-prompt').nextElementSibling;
                            if (nextSection && (nextSection.classList.contains('neofetch-style') || 
                                               nextSection.classList.contains('skills-and-projects') || 
                                               nextSection.classList.contains('git-activity') || 
                                               nextSection.classList.contains('thoughts-section') || 
                                               nextSection.classList.contains('connect-section'))) {
                                setTimeout(() => {
                                    nextSection.style.opacity = '1';
                                    nextSection.style.transform = 'translateY(0)';
                                }, 200);
                            }
                        }
                    }
                    type();
                }, 200);
                
                // Stop observing this element
                typewriterObserver.unobserve(code);
            }
        });
    }, { threshold: 0.5 });
    
    // Set up typewriter for CLI prompts
    cliPrompts.forEach(code => {
        code.style.opacity = '0';
        typewriterObserver.observe(code);
    });
    
    // Fade-in animation for sections (triggered by typewriter completion)
    const sections = document.querySelectorAll('.neofetch-style, .skills-and-projects, .git-activity, .thoughts-section, .connect-section');
    
    sections.forEach((section, index) => {
        section.style.opacity = '0';
        section.style.transform = 'translateY(20px)';
        section.style.transition = `opacity 0.6s ease-out, transform 0.6s ease-out`;
    });
    
    // Newsletter form functionality
    const newsletterForm = document.querySelector('.cli-form');
    if (newsletterForm) {
        newsletterForm.addEventListener('submit', function(e) {
            e.preventDefault();
            const button = this.querySelector('.cli-button');
            const input = this.querySelector('.cli-input');
            
            button.textContent = 'SENDING...';
            button.style.background = 'var(--cli-yellow)';
            button.style.transform = 'scale(0.95)';
            
            setTimeout(() => {
                button.textContent = '✓ SENT';
                button.style.background = 'var(--cli-green)';
                button.style.transform = 'scale(1)';
                input.value = 'Thank you for subscribing!';
                input.disabled = true;
                input.style.color = 'var(--cli-green)';
            }, 1200);
            
            setTimeout(() => {
                button.textContent = 'SEND';
                button.style.background = '';
                button.style.transform = '';
                input.value = '';
                input.disabled = false;
                input.style.color = '';
                input.placeholder = 'your@email.com';
            }, 4000);
        });
    }
    
    // Add a subtle cursor blink to the ASCII art section
    const asciiArt = document.querySelector('.ascii-art');
    if (asciiArt) {
        const cursor = document.createElement('span');
        cursor.textContent = '▋';
        cursor.style.color = 'var(--terminal-primary)';
        cursor.style.animation = 'blink 1s infinite';
        cursor.style.marginLeft = '5px';
        asciiArt.appendChild(cursor);
        
        // Add keyframes for blink animation
        const style = document.createElement('style');
        style.textContent = `
            @keyframes blink {
                0%, 50% { opacity: 1; }
                51%, 100% { opacity: 0; }
            }
        `;
        document.head.appendChild(style);
    }

    // ========================================================================
    // TERMINAL COLOR THEME SWITCHER
    // ========================================================================
    const themes = ['lavender', 'amber', 'cyan', 'green', 'white'];
    const themeNames = {
        lavender: 'Lavender (Synthwave)',
        amber: 'Amber (IBM 3278)',
        cyan: 'Cyan (VT220)',
        green: 'Green (Matrix)',
        white: 'White (Unix)'
    };
    
    // Load saved theme or default to lavender
    const savedTheme = localStorage.getItem('terminal-theme') || 'lavender';
    document.documentElement.dataset.terminalTheme = savedTheme;

    // Create and inject the theme switcher UI in the title meta area
    const codeToolsContainer = document.querySelector('.code-tools-button');
    const titleBlock = document.querySelector('.quarto-title-block');
    const titleMeta = titleBlock ? (titleBlock.querySelector('.quarto-title-meta') || titleBlock) : null;
    const targetContainer = titleMeta || (codeToolsContainer ? codeToolsContainer.parentElement : null);
    
    if (targetContainer) {
        const switcher = document.createElement('div');
        switcher.className = 'terminal-theme-switcher';
        switcher.innerHTML = `
            <span class="theme-label">Terminal</span>
            <div class="theme-options">
                ${themes.map(t => `
                    <button class="theme-dot ${t === savedTheme ? 'active' : ''}" 
                            data-theme="${t}" 
                            title="${themeNames[t]}"
                            aria-label="Switch to ${themeNames[t]} theme"></button>
                `).join('')}
            </div>
        `;
        
        // Insert into the title meta area to avoid overlapping the Code button
        targetContainer.appendChild(switcher);

        // Add click handlers
        switcher.querySelectorAll('.theme-dot').forEach(dot => {
            dot.addEventListener('click', () => {
                const theme = dot.dataset.theme;
                document.documentElement.dataset.terminalTheme = theme;
                localStorage.setItem('terminal-theme', theme);
                
                // Update active states
                switcher.querySelectorAll('.theme-dot').forEach(d => d.classList.remove('active'));
                dot.classList.add('active');
                
                // Update ASCII cursor color
                const cursor = document.querySelector('.ascii-art span');
                if (cursor) {
                    cursor.style.color = 'var(--terminal-primary)';
                }
            });
        });
    }
});
</script>
```